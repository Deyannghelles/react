/* const listaDeItens = [
    { id: 1, nome: 'maçã' },
    { id: 2, nome: 'banana' },
    { id: 3, nome: 'uva' },
    { id: 4, nome: 'pera' },
    { id: 5, nome: 'manga' }
];

function ListaDeFrutas() {
    return (
        <ul>
            {listaDeItens.map(item => (
                <li key={item.id}>{item.nome}</li>
            ))}
        </ul>
    );
}

export default ListaDeFrutas
 */




